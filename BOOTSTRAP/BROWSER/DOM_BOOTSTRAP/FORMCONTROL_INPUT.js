BOOTSTRAP.FORMCONTROL_INPUT = CLASS({

    preset : function() {'use strict';
        return BOOTSTRAP.INPUT;
    },

    params : function() {'use strict';
        return {
            c : 'form-control'
        };
    }
});
