BOOTSTRAP.FORMGROUP = CLASS({

    preset : function() {'use strict';
        return BOOTSTRAP.DIV;
    },

    params : function() {'use strict';
        return {
            c : 'form-group'
        };
    }
});
